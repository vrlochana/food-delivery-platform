server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: "jdbc:postgresql://localhost:5432/fooddelivery?options=-c%20TimeZone=UTC"
    username: admin
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true